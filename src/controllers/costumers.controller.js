import chalk from 'chalk';

export const indexCostumers = async () => {
  const { cpf, offset, limit, order, desc } = req.Params;

  console.log(chalk.cyan('GET /costumers'));
  try {

  } catch (error) {

  }
};
